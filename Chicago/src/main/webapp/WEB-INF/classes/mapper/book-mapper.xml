<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sist.dao.mapper.BookMapper">
	<select id="bookAllList" resultType="bookVO">
		SELECT book_code, book_name,
		book_category, writer, publisher, book_content, price, publication, img
		FROM goods1
	</select>

	<!-- book 메인화면 : 카테고리 별 신간 5권씩 출력 -->
	<select id="newBookCate1" resultType="bookVO">
		SELECT book_name, publisher, book_content, price, publication, img, writer, num FROM
		(SELECT book_name, publisher, book_content, price, publication, img, writer, rownum as num FROM
    	(SELECT book_name, publisher, book_content, price, publication, img, writer FROM GOODS1 where book_category='1' ORDER BY publication DESC)) 
    	WHERE num BETWEEN 1 AND 5
	</select>
		<select id="newBookCate2" resultType="bookVO">
		SELECT book_name, publisher, book_content, price, publication, img, writer, num FROM
		(SELECT book_name, publisher, book_content, price, publication, img, writer, rownum as num FROM
    	(SELECT book_name, publisher, book_content, price, publication, img, writer FROM GOODS1 where book_category='2' ORDER BY publication DESC)) 
    	WHERE num BETWEEN 1 AND 5
	</select>
		<select id="newBookCate3" resultType="bookVO">
		SELECT book_name, publisher, book_content, price, publication, img, writer, num FROM
		(SELECT book_name, publisher, book_content, price, publication, img, writer, rownum as num FROM
    	(SELECT book_name, publisher, book_content, price, publication, img, writer FROM GOODS1 where book_category='3' ORDER BY publication DESC)) 
    	WHERE num BETWEEN 1 AND 5
	</select>
		<select id="newBookCate4" resultType="bookVO">
		SELECT book_name, publisher, book_content, price, publication, img, writer, num FROM
		(SELECT book_name, publisher, book_content, price, publication, img, writer, rownum as num FROM
    	(SELECT book_name, publisher, book_content, price, publication, img, writer FROM GOODS1 where book_category='4' ORDER BY publication DESC)) 
    	WHERE num BETWEEN 1 AND 5
	</select>
</mapper>